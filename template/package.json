{
  "name": "ai-dungeon-typescript-starter",
  "version": "0.1.0",
  "main": "dist/context.js",
  "scripts": {
    "build": "node scripts/ensure-library-exports.js && tsc && rollup -c rollup.config.mjs && node scripts/bundle.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "ai-dungeon-sdk": "latest"
  },
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^16.0.1",
    "@rollup/plugin-typescript": "^12.1.4",
    "esbuild": "^0.20.0",
    "rollup": "^4.44.2",
    "typescript": "^5.0.0"
  }
}
